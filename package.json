{
  "name": "asistente-operativo",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "build": "esbuild src/admin.js src/cloud.js --bundle --minify --target=es2022 --outdir=docs --entry-names=[name]-bundle",
    "test:access": "node tests/access.test.mjs",
    "test:rules": "node tests/firestore.test.cjs"
  },
  "dependencies": {
    "firebase": "12.19.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "esbuild": "0.25.10"
  }
}
