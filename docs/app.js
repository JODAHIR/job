function message(text,kind,result){const article=document.createElement('article');article.className='message '+kind;if(kind==='bot'){const icon=document.createElement('div');icon.className='avatar';icon.textContent='a.';article.append(icon)}const bubble=document.createElement('div');bubble.className='bubble';const p=document.createElement('p');p.textContent=text;bubble.append(p);for(const value of result?.templates??[]){const quote=document.createElement('p');quote.textContent=value;bubble.append(quote);const btn=document.createElement('button');btn.className='copy';btn.textContent='Copiar plantilla';btn.onclick=async()=>{try{await navigator.clipboard.writeText(value);btn.textContent='Copiada'}catch{btn.textContent='Seleccioná el texto para copiar'}};bubble.append(btn)}if(result?.source){const source=document.createElement('p');source.className='source';source.textContent='Fuente: '+result.source;bubble.append(source)}article.append(bubble);document.querySelector('#chat').append(article);article.scrollIntoView({block:'end'});}
async function ask(value){if(typeof value!=='string'||!value.trim()||value.length>1000)throw new Error('Escribí una consulta de entre 1 y 1000 caracteres.');const q=value.trim();message(q,'user');const result=window.firebaseAnswer?await window.firebaseAnswer(q):{text:"La conexión con Firebase todavía se está cargando. Volvé a intentarlo en unos segundos."};message(result.text,'bot',result);return result}
document.querySelector('#form').addEventListener('submit',e=>{e.preventDefault();const input=document.querySelector('#question');if(input.value.trim()){ask(input.value);input.value='';input.focus()}});document.querySelectorAll('[data-question]').forEach(b=>b.onclick=()=>ask(b.dataset.question));
if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'consultar_procedimiento',description:'Agrega una consulta y su respuesta guiada a la conversación visible.',inputSchema:{type:'object',properties:{consulta:{type:'string',minLength:1,maxLength:1000}},required:['consulta'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>ask(input?.consulta)})).catch(()=>{})}catch{}}
